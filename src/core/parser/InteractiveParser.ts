import { IInteractiveParser } from "./IInteractiveParser";

export class InteractiveParser implements IInteractiveParser {
    parse(_args: string[]) {
        throw new Error('Not implemented');
    }
}
